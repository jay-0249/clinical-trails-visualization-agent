[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "integration: hits the live ClinicalTrials.gov API (deselected by default)",
]
# Keep the default suite hermetic and fast; run live tests with `-m integration`.
addopts = "-m 'not integration'"
